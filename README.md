# Advanced-Spam-Detection-Model

This project implements a spam detection model that classifies messages as spam or ham (not spam) using Natural Language Processing (NLP) and a simple neural network built with NumPy. The dataset consists of SMS messages labeled as spam or ham.
